// These are public values — safe to hardcode. NEXT_PUBLIC_ env vars
// are inlined at build time by Next.js, so fallbacks don't work as expected.
export const projectId = 'o6g3f59e';
export const dataset = 'production';
export const apiVersion = '2024-01-01';
